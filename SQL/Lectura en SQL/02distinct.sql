SELECT distinct * users ---Seleccionar todo los datos distintos de la carpeta users---

SELECT distinct age FROM users; ---Me traes las edades que se repiten o no, pero sólo 1

SELECT distinct users_id FROM users; ---Me traes el id que se repiten o no, pero sólo 1
